/****************************************
* Trigger Name : CH_WorkgroupScheduleTimeslotTrigger_Test 
* Created Date: 18-09-2019
* Created By : Sonia Reis
*****************************************/
@isTest
public class CH_WorkgroupScheduleTimeslotTrigger_Test {
	/*
	
	@testSetup static void methodName() {
		CH_UTIL_TEST.setupCase(false);
		CH_UTIL_TEST.createWorkgroupSchedules();
	}
	
	@isTest static void testMethodUpdate() { 
		try{
			List<CH_Workgroup_Schedule_Timeslot__c> tsList = [SELECT Id, CH_Workgroup_Schedule__c, CH_Workgroup_Id__c,  CH_End_Time__c  FROM CH_Workgroup_Schedule_Timeslot__c ];
			tsList[0].CH_End_Time__c = time.newInstance(22, 59, 11, 11);
			update tsList[0];
		}
		catch(Exception e ){
			//TO DO
		}
	}
	
	@isTest static void testMethodDelete() { 
		try{
			List<CH_Workgroup_Schedule_Timeslot__c> tsList = [SELECT Id, CH_Workgroup_Schedule__c, CH_Workgroup_Id__c,  CH_End_Time__c  FROM CH_Workgroup_Schedule_Timeslot__c ];
			delete tsList[0];
		}
		catch(Exception e ){
			//TO DO
		}
	}
	
	 @isTest static void testMethodDelete2() { 
		try{
			test.startTest();
			List<CH_Workgroup_Schedule_Timeslot__c> tsList = [SELECT Id, CH_Workgroup_Schedule__c, CH_Workgroup_Id__c  FROM CH_Workgroup_Schedule_Timeslot__c ];
			CH_UTIL_TEST.createWGSTimeslotAlone(tsList[0].CH_Workgroup_Id__c, tsList[0].CH_Workgroup_Schedule__c);
			delete tsList[0];
			test.stopTest();
		}
		catch(Exception e){
			//TO DO
		}
	}*/
	

}